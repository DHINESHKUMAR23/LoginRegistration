@model LoginRegistration.Models.Cours

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Cours</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.CourseId)

        @Html.HiddenFor(model => model.Degree)

        @Html.HiddenFor(model => model.Specialization)

        @Html.HiddenFor(model => model.DurationInYears)

        @Html.HiddenFor(model => model.Min_APS)

        @Html.HiddenFor(model => model.DateOfAddition)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
